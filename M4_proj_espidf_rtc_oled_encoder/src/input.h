#pragma once

#include "app.h"

// Обробляє весь ввід з енкодера за один тік: застосовує накопичену дельту
// обертання (навігація фокусом між полями або зміна значення поля, що
// редагується) і клік (вибір / вхід-вихід з редагування / Back)
void handle_encoder_input(app_data_t *app_data);
